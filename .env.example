### Frontend environment configuration
### Copy to `.env` or set these variables before running `npm run dev`

# Point the dashboard UI at your backend (match the running backend host/port).
VITE_API_BASE_URL=https://doorspital-backend.onrender.com

# Optional: pre-seed an admin token for development (use a valid JWT from backend).
VITE_ADMIN_TOKEN=
